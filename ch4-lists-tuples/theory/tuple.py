# TUPLE - A tuple is a built-in data type used to store a collection of items in a single variable. 
# It is similar to a list, but with one key difference:
# 👉 Tuples are immutable (they cannot be changed after creation).

a = (2,3,5,"helllo","good")
print(a)

print(type(a))